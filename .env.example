# For local development copy to .env.local and fill in.
# NOT needed in production (panel inside the HA iframe) — auth comes from loader.js.
VITE_HASS_URL=http://home.local
VITE_HASS_TOKEN=your-long-lived-access-token
