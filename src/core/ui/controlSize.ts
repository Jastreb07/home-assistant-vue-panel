/**
 * Size scale shared by Button, Input and SelectMenu — the actual values
 * live in the theme's main.css as `.vp-size-<size>` token classes.
 */
export type ControlSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl'
